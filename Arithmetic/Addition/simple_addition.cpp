#include <iostream>

using namespace std;

int main()
{
    int a, b, c, sum;

    cout << "Enter any two numbers: ";
    cin >> a >> b >> c;

    sum = a + b + c;
    cout << a << " + " << b << " + " << c << " = " << sum;

    return 0;
}
